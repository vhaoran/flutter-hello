# list
    dynamic
    List.map((e)=>{}).toList()
    List.forEach()
    List.Where***
    List.Find***
    List.sort();
    List.add()/insert
    list.length()/len
# map
  m := {
  "a":2,
  "b":3
  }
# 回调
 callback
 

